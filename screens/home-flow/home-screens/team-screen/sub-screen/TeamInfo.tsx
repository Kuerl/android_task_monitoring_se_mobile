import React from "react";
import { Text, StyleSheet } from "react-native";

const TeamInfo: React.FC = () => {
  return <Text>Team Info</Text>;
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
});

export default TeamInfo;
